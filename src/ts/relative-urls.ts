jQuery(document).ready(function($) {
  $.each($('a'), function() {
    /**
      let url = $(this).attr('href');
      url = url.replace(
      /^(https?:\/\/(localhost|bacondev|baconstage)[a-zA-z0-9-.:]+)/gi,
      ''
    );
    $(this).attr('href', url);
    */
  });
});
